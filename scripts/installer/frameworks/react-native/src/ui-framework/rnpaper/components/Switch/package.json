{
	"name": "@react-native-jsonschema-form-components/material-switch",
	"version": "0.0.1",
	"author": "Vipin Tanna",
	"main": "dist/index.js",
    "scripts": {
        "build": "rm -rf dist && tsc",
        "prepublishOnly": "npm run build"
    }
}
