// Library
import React from 'react';
import CircularProgress from '@mui/material/CircularProgress';

const CrossPlatformLoadingWrapper = () => (
    <div> 
      <CircularProgress disableShrink />
    </div>
);

export default CrossPlatformLoadingWrapper;
