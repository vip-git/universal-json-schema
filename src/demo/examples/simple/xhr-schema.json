{
  "ui:page": {
    "onload": {
      "xhr:datasource": {
        "url": "https://60154dbe55dfbd00174ca231.mockapi.io/api",
        "method": "GET",
        "headers": {},
        "payload": {},
        "map:results": {
          "firstName": "${first_name}",
          "lastName": "${last_name}",
          "customComponent": "${custom_component}",
          "customRating": "${custom_rating}",
          "currency": "${currency}",
          "default": "${default}",
          "radio": "${radio}",
          "select": "${select}",
          "react-select": "${react_select}",
          "age": "${age}",
          "upload": "${upload}",
          "multiSelect": "${multi_select}",
          "creatableSelectTest": "${creatable_select_test}",
          "selectTest": "${select_test}",
          "bio": "${bio}",
          "date": "${date}",
          "password": "${password}",
          "telephone": "${telephone}"
        }
      }
    },
    "onsubmit": {}
  },
  "selectTest": {
    "onclick": {
      "enum": {
        "xhr:datasource": {
          "url": "",
          "method": "POST",
          "headers": {},
          "payload": {},
          "apiSchemaSource": "",
          "mapResults": []
        }
      }
    }
  }
}