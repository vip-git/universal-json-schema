{
  "ui:page": {
    "onload": {
      "xhr:datasource": {
        "url": "https://news123.free.beeceptor.com",
        "method": "GET",
        "headers": {},
        "payload": {},
        "map": {
          "formData" : {
            "firstName": "${first_name}",
            "lastName": "${last_name}",
            "customComponent": "${custom_component}",
            "customRating": "${custom_rating}",
            "currency": "${currency}",
            "default": "${default}",
            "radio": "${radio}",
            "select": "${select}",
            "react-select": "${react_select}",
            "age": "${age}",
            "upload": "${upload}",
            "multiSelect": "${multi_select}",
            "creatableSelectTest": "${creatable_select}",
            "selectTest": "${select_test}",
            "bio": "${bio}",
            "date": "${date}",
            "password": "${password}",
            "telephone": "${telephone}"
          },
          "uiData": {
            "customComponent": "${custom_component.start_date} - ${custom_component.end_date}",
            "currency": "123123.123.123123.123"
          }
        }
      }
    },
    "onsubmit": {}
  },
  "selectTest": {
    "onclick": {
      "enum": {
        "xhr:datasource": {
          "url": "",
          "method": "POST",
          "headers": {},
          "payload": {},
          "apiSchemaSource": "",
          "map": []
        }
      }
    }
  }
}