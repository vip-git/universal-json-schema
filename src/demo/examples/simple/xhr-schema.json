{
  "ui:page": {
    "onload": {
      "xhr:datasource": {
        "url": "https://60154dbe55dfbd00174ca231.mockapi.io/api/2",
        "method": "GET",
        "headers": {},
        "payload": {},
        "map:results": "#/definitions/results"
      }
    },
    "onsubmit": {
      "xhr:datasource": {
        "url": "https://60154dbe55dfbd00174ca231.mockapi.io/api/2",
        "method": "PUT",
        "headers": {},
        "map:payload": {
          "string": {
            "firstName:first_name": "${firstName}",
            "lastName:last_name": "${lastName}",
            "select": "${select}",
            "react-select:react_select": "${react-select}",
            "upload": "${upload}",
            "bio": "${bio}",
            "date": "${date}",
            "password": "${password}",
            "telephone": "${telephone}"
          },
          "integer": {
            "customRating:custom_rating": "${customRating}",
            "age": "${age}"
          },
          "number": {
            "currency": "${currency}"
          },
          "boolean": {
            "default": "${default}",
            "radio": "${radio}"
          },
          "array": {
            "multiSelect": "${multiSelect}",
            "creatableSelectTest:creatable_select_test": "${creatableSelectTest}",
            "selectTest:select_test": "${selectTest}"
          },
          "object": {
            "customComponent:custom_component": {
              "startDate:start_date": "${customComponent.startDate}",
              "endDate:end_date": "${customComponent.endDate}"
            }
          }
        },
        "map:results": "#/definitions/results"
      }
    }
  },
  "properties": {
    "array": {
      "selectTest": {
        "onload": {
          "xhr:datasource": {
              "url": "https://60154dbe55dfbd00174ca231.mockapi.io/api",
              "method": "GET",
              "headers": {},
              "payload": {},
              "map:items.enum": "name"
          }
        }
      }
    }
  },
  "definitions": {
    "results": {
      "string": {
        "firstName": "${first_name}",
        "lastName": "${last_name}",
        "select": "${select}",
        "react-select": "${react_select}",
        "upload": "${upload}",
        "bio": "${bio}",
        "date": "${date}",
        "password": "${password}",
        "telephone": "${telephone}"
      },
      "integer": {
        "customRating": "${custom_rating}",
        "age": "${age}"
      },
      "number": {
        "currency": "${currency}"
      },
      "boolean": {
        "default": "${default}",
        "radio": "${radio}"
      },
      "array": {
        "multiSelect": "${multiSelect}",
        "creatableSelectTest": "${creatable_select_test}",
        "selectTest": "${selectTest}"
      },
      "object": {
        "customComponent": "${customComponent}"
      }
    }
  }
}
