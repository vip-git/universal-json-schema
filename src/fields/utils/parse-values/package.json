{
  "name": "@react-jsonschema-form-utils/parse-values",
  "version": "2.0.1",
  "author": "Vipin Tanna",
  "main": "dist/index.js",
  "scripts": {
      "build": "rm -rf dist && tsc",
      "prepublishOnly": "npm run build"
  }
}
