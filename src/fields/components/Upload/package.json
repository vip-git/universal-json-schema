{
	"name": "@react-jsonschema-form-components/upload",
	"version": "3.0.0",
	"author": "Vipin Tanna",
	"main": "dist/index.js",
    "scripts": {
        "build": "rm -rf dist && tsc",
        "prepublishOnly": "npm run build"
    }
}
